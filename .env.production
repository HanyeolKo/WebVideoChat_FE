# 프로덕션은 호스트 nginx(443)가 /api·/socket 경로를 백엔드로 프록시한다.
# 따라서 FE는 상대경로를 사용하며, 빌드 시점에 백엔드 호스트를 굽지 않는다(비워 둠).
# 덕분에 동일 FE 이미지를 환경에 무관하게 재사용할 수 있고, BE 주소 변경은 nginx 설정만 바꾸면 된다.
VITE_API_BASE_URL=
VITE_WS_BASE_URL=
